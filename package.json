{
  "dependencies": {
    "ejs": "^3.1.10"
  },
  "scripts": {
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "ts-node-dev -r tsconfig-paths/register src/server.ts",
    "copy-templates": "copyfiles -u 1 src/utils/mail/templates/*.ejs dist/utils/mail/templates"
  }
}
